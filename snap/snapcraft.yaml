name: fx
version: 30.0.1
summary: Terminal JSON viewer
description: Terminal JSON viewer
base: core20
grade: stable
confinement: strict
architectures:
  - build-on: armhf
  - build-on: amd64
  - build-on: arm64

# TODO: Allow to read .fxrc.js from home directory
#plugs:
#  home:
#    interface: personal-files
#    read:
#      - $HOME/.fxrc.js
#  network:
#    interface: network

apps:
  fx:
    command: bin/fx
    plugs: [ home, network ]

parts:
  fx:
    plugin: go
    source: https://github.com/antonmedv/fx.git
    source-type: git
    stage-snaps:
      - node/18/stable
